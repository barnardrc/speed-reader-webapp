Place your landing demo media here.

- Default drop-in path: `web/public/landing-demo.gif`
- Optional override: set `VITE_LANDING_DEMO_URL` in `web/.env`

Supported media:
- GIF via `<img>`
- MP4/WebM/Ogg via `<video>`
